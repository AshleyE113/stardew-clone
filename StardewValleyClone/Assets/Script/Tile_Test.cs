using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tile_Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Hey! This is a test script file only to check if I'm working on the branch or not!
        //And whether I can merge it in main!
        //Ignore or delete this later!
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
